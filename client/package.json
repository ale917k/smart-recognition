{
  "name": "smart-recognition-client",
  "dependencies": {
    "clarifai": "^2.9.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1",
    "react-particles-js": "^2.7.1",
    "react-scripts": "^4.0.0",
    "react-tilt": "^0.1.4",
    "tachyons": "^4.12.0"
  },
  "scripts": {
    "build": "react-scripts build",
    "bundle": "npm build -- --stats; webpack-bundle-analyzer build/bundle-stats.json",
    "start": "react-scripts start",
    "test": "react-scripts test"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "proxy": "http://localhost:8080/"
}
